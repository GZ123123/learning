import { TodoHighlightDirective } from './todo-highlight.directive';

describe('TodoHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new TodoHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
