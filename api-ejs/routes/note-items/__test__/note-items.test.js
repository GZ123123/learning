const { uid } = require("uid");
const assert = require("assert");

const getById = require("getById");
const create = require("create");
const update = require("update");
const _delete = require("delete");

describe("Test Note Items", () => {
	var id;
	describe("Create", () => {
		it("Will Create A Note Items", () => {
			// save id
		});
		it("Should Have Note Items", () => {});
	});
	describe("Update", () => {
		it("Will Update A Note Items", () => {});
		it("Should Be Updated", () => {});
	});
	describe("Delete", () => {
		it("Note Item Will Be Remove", () => {});
	});
});
